<div class="row justify-content-center">
  <h1><span id="invoice-name"><%= @invoice.invoice_name %></span></h1>
</div>
<hr>

<div id="invoice-navbar-forms">
  <%= render 'invoices/nav_bar', invoice: @invoice %>
</div>

<%= render 'skus/new', invoice: @invoice %>

<hr>

<div id="items-table">
  <%= render 'invoices/items/table', invoice: @invoice %>
</div>
