<div class="card-header border-0 bg-transparent p-1">
  <div class="form-row mb-1">
    <button id="<%= dom_ref('caret', card_id) %>" class="<%= boot_classes(:caret, 'mx-1', 'my-0', 'pl-1', 'py-0', 'pt-2') %>" data-target="#<%= dom_ref('target', card_id) %>" type="button"><%= (fa_icon "caret-right") %></button>
    <div class="col pt-2">
      <%= header %>
    </div>
  </div>
</div>
