<%= form_with model: item, url: item_artists_path, method: "post", id: "post-item-artist", class: "collapse" do |f| %>
  <%= number_field_tag :artist_id, artist.try(:id) %>
  <%= number_field_tag :id, item.id %>
<% end %>
